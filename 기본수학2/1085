#include <iostream>
#include <cmath>

int main(){
    using namespace std;
    int x,y,w,h;
    cin >> x >> y >> w >> h;
    double shortest = x;
    if (shortest> y) shortest=y;
    if (shortest> w-x) shortest=w-x;
    if (shortest> h-y) shortest=h-y;
    
    cout << shortest << endl;
    
}

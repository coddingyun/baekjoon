#include <iostream>

int main(){
    using namespace std;
    long long A,B,C;
    cin >> A >> B >> C;
    long long result = A/(C-B);
    if (C<=B ) cout << -1 << endl;
    else cout << result +1 << endl;
}
